{% extends 'parent.html.twig' %}

{% block stylesheets %}
    <link href="{{ asset('web/styles/messages.css') }}" rel="stylesheet" type="text/css"/>
    <link href="{{ asset('web/styles/style.css') }}" rel="stylesheet" type="text/css"/>
    <script src="{{ asset('web/scripts/jquery-3.4.1.min.js') }}"></script>
    <title>Wiadomości</title>
{% endblock %}



{% block body %}

    {% if shortcuts is empty %}
    <h3 style="text-align: center; color: darkgrey;">Nie posiadasz żadnych wiadomości.</h3>
    {% endif %}
    {% for shortcut in shortcuts %}
    <div class="comment">
        <a href="{{ path('profileIndexAction', {'username': shortcut.username } ) }}"><img id="comment-avatar-left"  src="{{ asset(photo_data_provider.PhotoByUsername(shortcut.username)) }}" width="70" height="70"></a>



        <p class="project-title"
           href=" ">{{ shortcut.username }}</p>
        ✽ {{ shortcut.date }}


        <p class="comment-content">

            {{ shortcut.lastMessageUsername }}:

            {{ shortcut.lastMessageText }}

        </p>

        <button class="button" onclick="location.href='{{ path('messagesIndexAction', {'userId':  shortcut.userId }) }}'"
        >
            Przejdź do konwersacji
        </button>

    </div>
    {% endfor %}



    <div class="comment">
        <a href=" "> <img id="comment-avatar-left" src="" width="70" height="70"></a>


        <a class="project-title"
           href="  ">username</a>
        prosi o dodanie do <a class="project-title"
                              href="  ">nazwa zespolu</a>
        ✽ czas


        <p class="request-content">tekst</p>

        <input type="hidden" name="decision" value="decline">
        <input type="hidden" name="userId" value="">
        <input type="hidden" name="projectId" value="">
        <input type="hidden" name="addUserToProjectRequestId" value="">

        <button class="button" type="submit" style="Background-color: darkred;"> Odrzuć</button>


        <input type="hidden" name="decision" value="accept">
        <input type="hidden" name="userId" value="">
        <input type="hidden" name="projectId" value="">
        <input type="hidden" name="addUserToProjectRequestId" value="">
        <button class="button" style="background-color: darkgreen;"> Zaakceptuj</button>
        </form>
    </div>

    <script>
        $(document).ready(function () {
        });


    </script>



{% endblock %}